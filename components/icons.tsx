import { Home, User, Repeat, Upload, Share2, Wallet } from "lucide-react"
// 移除了 eth 和 usdc 的 SVG 定义

export const Icons = {
  home: Home,
  user: User,
  swap: Repeat,
  withdraw: Upload,
  share: Share2,
  wallet: Wallet,
  // eth 和 usdc 的 SVG 定义已移除，因为现在直接使用图片
}
